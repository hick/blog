<% provide(:title, '文章列表') %>

<h1>最新文章列表</h1>
 

 
<ul>
  <% @posts.each do |post| %>
    <li><%= raw(post.created_at.localtime.to_s(:db)) %> <%= link_to post.title, "#{root_path}#{post.title}" %></li>
  <% end %>
</ul>

<%= link_to '查看更多文章', posts_path %> 
